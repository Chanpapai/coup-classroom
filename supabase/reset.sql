-- ============================================================
-- รีเซ็ตห้อง/ผู้เล่นทั้งหมด — รันหลัง deploy เกมเวอร์ชันใหม่ทุกครั้ง
-- ป้องกันห้องเก่า (โครงสร้าง state แบบเก่า) ค้างอยู่แล้วพัง
--
-- วิธีใช้: Supabase Dashboard → SQL Editor → New query → วางไฟล์นี้ → Run
-- (ปลอดภัย ลบแค่ข้อมูลห้อง/มือผู้เล่น ไม่กระทบ schema หรือผู้ใช้ที่ล็อกอินไว้)
-- ============================================================

truncate table public.room_private;
truncate table public.player_views;
truncate table public.rooms;
